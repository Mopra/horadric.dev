[package]
name = "horadric-hooks"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Receives Claude Code hook events over localhost and installs the hooks that send them"

[dependencies]
horadric-core = { path = "../horadric-core" }
serde_json = { workspace = true }
